/target
.direnv
*.tiff
nkscan-wb.txt
result

# LibreOffice lock files, from opening the specs
.~lock.*#

# The scratch dump protocol::decode's round-trip test reads when it is there
scan.raw

# Scratch dumps and logs a debugging run leaves behind
ours.log
*.log
thumb.raw
thumb.ppm
thumb.pgm
*.ppm
*.pgm
